name: Add-on auto-update

on:
  push:
    branches: [ "master" ]
    paths:
      - repository/*.zip

  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 2

      - name: Get last commit
        id: last-commit
        run: |
          echo "::set-output name=commit::$(git rev-parse HEAD)"

      - name: Get modified add-ons
        id: modified-addons
        run: |
          echo "::set-output name=list::$(for path in `git diff --diff-filter=M --name-only HEAD~1..HEAD -- repository/` ; do basename $path ; done)"

      - name: Run index file updater for each updated add-on
        run: |
          chmod +x scripts/update_addon_index.sh
          for file in ${{ steps.modified-addons.outputs.list }}; do
            echo "Executing script for modified add-on ${file}."
            scripts/update_addon_index.sh -f $file -c ${{ steps.last-commit.outputs.commit }}
          done
